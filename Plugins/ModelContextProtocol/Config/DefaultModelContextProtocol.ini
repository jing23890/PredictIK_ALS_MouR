; Configure the project port in Edit > Project Settings > Plugins > MCP for Unreal Editor.
; UE_MCP_PORT can temporarily override it for one editor process. Authentication tokens
; remain environment-only so secrets are never persisted in a distributable plugin.

[/Script/ModelContextProtocol.ModelContextProtocolSettings]
Port=18777

[ModelContextProtocol]
SchemaVersion=1
